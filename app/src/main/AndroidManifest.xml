<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="company.product.dpop.de_pop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".notice.notice_activities.DepartmentActivity" />
        <activity android:name=".notice.notice_activities.HODActivity" />
        <activity android:name=".register.StudentRegisterActivity" />
        <activity android:name=".login.LoginActivity"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register.TeacherRegisterActivity" />
        <activity android:name=".login.RegisterPickerActivity" />

        <activity android:name=".querybench.query_activites.TechnicalQueryActivity" />
        <activity android:name=".querybench.query_activites.SubjectQueryActivity"></activity>
    </application>

</manifest>